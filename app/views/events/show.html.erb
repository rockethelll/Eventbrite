<div class="container-fluid text-center">
  <h1><%= @event.title%></h1>
  <p><%= @event.description %></p>
  <p>Le mail de l'organisateur de l'événement est : <%= @event.admin.email %></p>
  <p>Date de début : <%= @event.start_date %></p>
  <p>Date de début : <%= @event.start_date + @event.duration.minutes %></p>
  <p>Lieu : <%= @event.location %></p>
  <p>Prix : <%= @event.price %> €</p>
  <p>Il y a de personnes inscrites</p>
  <%= link_to "Modifier l'évenement", edit_event_path, class: "btn btn-sm btn-primary"%>
  <%= link_to "Voir les participants", attendance_path, class: "btn btn-sm btn-primary"%>

  <div>
    <%= image_tag @event.picture %>
  </div>

  <%= button_to "Passer commande (NEXT GEN)", checkout_create_path(total: 10), class: "btn btn-primary mt-5", remote: true, data:{turbo: false} %>
</div>

