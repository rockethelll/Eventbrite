<div class="container">
  <h1 class="my-4">Modidfer vos informations</h1>

  <%= form_with model: @user, multipart: true do |form| %>
    <div class="form-group">
      <%= form.label :prénom %>
      <%= form.text_field :first_name, class: 'form-control mb-3' %>
    </div>
    <div class="form-group">
      <%= form.label :nom %>
      <%= form.text_field :last_name, class: 'form-control mb-3' %>
    </div>

    <div class="form-group">
      <%= form.label :hobbies %>
      <%= form.text_area :description, class: 'form-control', rows: 5, placeholder: "Contenu de l'article" %>
    </div>

    <div class="form-group text-center">
      <%= form.submit "Modifier le profil", class: "btn btn-success mt-5" %>
    </div>

    <div class="form-group text-center">
      <%= file_fied_tag "Ajouter une photo de profil", class: "btn btn-success mt-5" %>
    </div>
  <% end %>
</div>